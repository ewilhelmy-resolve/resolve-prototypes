# Hello World Container

This is a simple demo repository that:

- Builds a Docker image that prints "Hello from inside the container!"
- Pushes the image to GitHub Container Registry (GHCR)
- SSHes into an internal host (`onboarding.resolve.io`) and runs the container
- Fully automated via GitHub Actions
