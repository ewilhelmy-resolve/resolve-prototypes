# Local development environment variables (safe to commit)
#
# Cursor blocks editing `.env` via global ignore rules, so the API uses this file
# for local development (see `packages/api-server/package.json`).

# API
PORT=3000
APP_URL=http://localhost:3000
CLIENT_URL=http://localhost:5174
LOG_LEVEL=debug

# Postgres (docker-compose.yml)
DATABASE_URL=postgres://rita:rita@localhost:5432/onboarding

# Valkey/Redis (docker-compose.yml uses valkey)
VALKEY_URL=redis://localhost:6379

# RabbitMQ (docker-compose.yml)
RABBITMQ_URL=amqp://guest:guest@localhost:5672

# Keycloak (docker-compose.yml)
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=rita-chat-realm

# Mock service
MOCK_SERVICE_PORT=3001

