# API Configuration
# Leave empty for development (uses Vite proxy)
# Set to full URL for production (e.g., https://api.yourapp.com)
VITE_API_URL=

# Pendo Analytics (Optional)
# Get your API key from: https://app.pendo.io → Settings → App Settings → App Details
# If not set, Pendo will be disabled (app functions normally without analytics)
# Create separate Pendo apps for dev/staging/production environments
VITE_PENDO_API_KEY=